## mendpoint: adopt Stripe listAutoPaging — new_capability (optional)

**Label:** optional improvement / feature adoption

Replace manual `has_more` + `starting_after` loop with official auto-paging helper for simpler, safer iteration.
