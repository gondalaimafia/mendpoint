# Example 06 — Go Stripe pagination

Go consumer using `CustomerListParams.StartingAfter` (stripe-go style) that must migrate when the list cursor field renames.

Quality bar: `npm run phase-d:go` (≥70% expected-site recall).
