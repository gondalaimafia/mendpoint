# OpenAI max_tokens is deprecated

Chat completions warn that **`max_tokens` is deprecated** and should be replaced with **`max_completion_tokens`**.

Please rename the parameter across our LLM client so we stop getting deprecation warnings and stay compatible with current models.
