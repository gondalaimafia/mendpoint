# Mendpoint Phase A consumer

Deliberately uses **Acme Payments v1** patterns (`amount_cents`, receipt endpoint) so the agent can open a real migration PR.

Do not "fix" this file by hand for demos — let the agent PR do it.
