{
  "tenantId": "tenant_default",
  "slug": "acme-payments",
  "captured": {
    "title": "mendpoint: migrate Acme Payments — breaking",
    "body": "## Acme Payments API migration\n**Risk:** `breaking`  \n**Confidence:** `high`  \n**Coverage:** `analyzed`  \n**Mode:** `migrate`  \n**Files edited:** **2**  \n**Agent policy:** opens a PR only — never commits to protected branches.\n### Summary\n[breaking] renamed request field amount_cents → amount on POST /v1/charges. removed /v1/charges/{id}/receipt. added /v1/balance. 4 other structural change(s).\n### Impact analysis\nConfirmed 20 site(s); types: direct_call, http_path, field_access. Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes. Optional adoption: new capability at /v1/balance Rename field usages amount_cents → amount on POST /v1/charges\n\n- Candidates discovered: **27**\n- Confirmed sites: **27**\n- Low-confidence notifications: **7**\n- Coverage: **analyzed** — 4 file(s) inspected; languages present: javascript, python, typescript; supported: javascript, python, typescript\n\n_Full coverage: this is complete evidence, not merely an empty result._\n\n### Impactable surfaces (sample)\n- `acme-payments./v1/charges/{id}/receipt.path_removed` (breaking) — Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n- `acme-payments./v1/balance.path_added` (new_capability) — Optional adoption: new capability at /v1/balance\n- `acme-payments.POST./v1/charges.request_field_renamed.amount_cents.amount` (breaking) — Rename field usages amount_cents → amount on POST /v1/charges\n- `acme-payments.POST./v1/charges.response_field_removed.amount_cents` (breaking) — Stop reading/writing field amount_cents; migrate consumers of that property.\n- `acme-payments.POST./v1/charges.response_field_added.amount` (new_capability) — Optional adoption: new capability at POST /v1/charges\n- `acme-payments.GET./v1/charges/{id}.response_field_removed.amount_cents` (breaking) — Stop reading/writing field amount_cents; migrate consumers of that property.\n- `acme-payments.GET./v1/charges/{id}.response_field_added.amount` (new_capability) — Optional adoption: new capability at GET /v1/charges/{id}\n\n### Provider docs\nhttps://docs.example.com/acme-payments\n### Confirmed edit sites\n- `src/payments.ts`\n- `src/webhooks.py`\n### Evidence\n- `src/payments.ts:28` **client.charges.create** (high, direct_call) — `client.charges.create`\n  - fix: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:6` **/v1/charges/{id}/receipt** (high, http_path) — `/v1/charges`\n  - fix: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:20` **/v1/charges/{id}/receipt** (high, http_path) — `/v1/charges/`\n  - fix: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n  - path: `src/payments.ts` (direct provider usage)\n- `src/webhooks.py:11` **/v1/charges/{id}/receipt** (high, http_path) — `/v1/charges/{charge_id}/receipt`\n  - fix: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/webhooks.py:20` **/v1/charges/{id}/receipt** (high, http_path) — `/v1/charges`\n  - fix: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/payments.ts:6` **/v1/charges** (high, http_path) — `/v1/charges`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:20` **/v1/charges** (high, http_path) — `/v1/charges/`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/webhooks.py:11` **/v1/charges** (high, http_path) — `/v1/charges/{charge_id}/receipt`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/webhooks.py:20` **/v1/charges** (high, http_path) — `/v1/charges`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/payments.ts:10` **amount_cents** (high, field_access) — `amount_cents: cents,`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:16` **amount_cents** (high, field_access) — `return res.json() as Promise<{ id: string; amount_cents: number; status: string }>;`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:26` **amount_cents** (high, field_access) — `charges: { create: (body: { amount_cents: number; currency: string }) => Promise<unknown> };`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:27` **amount_cents** (high, field_access) — `}, amount_cents: number, currency: string) {`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:28` **amount_cents** (high, field_access) — `return client.charges.create({ amount_cents, currency });`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/payments.ts` (direct provider usage)\n- `src/webhooks.py:17` **amount_cents** (high, field_access) — `def create_charge(amount_cents: int, currency: str) -> dict:`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/webhooks.py:18` **amount_cents** (high, field_access) — `payload = json.dumps({\"amount_cents\": amount_cents, \"currency\": currency}).encode()`\n  - fix: Rename field usages amount_cents → amount on POST /v1/charges\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/payments.ts:6` **/v1/charges/{id}** (high, http_path) — `/v1/charges`\n  - fix: Stop reading/writing field amount_cents; migrate consumers of that property.\n  - path: `src/payments.ts` (direct provider usage)\n- `src/payments.ts:20` **/v1/charges/{id}** (high, http_path) — `/v1/charges/`\n  - fix: Stop reading/writing field amount_cents; migrate consumers of that property.\n  - path: `src/payments.ts` (direct provider usage)\n- `src/webhooks.py:11` **/v1/charges/{id}** (high, http_path) — `/v1/charges/{charge_id}/receipt`\n  - fix: Stop reading/writing field amount_cents; migrate consumers of that property.\n  - path: `src/webhooks.py` (direct provider usage)\n- `src/webhooks.py:20` **/v1/charges/{id}** (high, http_path) — `/v1/charges`\n  - fix: Stop reading/writing field amount_cents; migrate consumers of that property.\n  - path: `src/webhooks.py` (direct provider usage)\n### What changed in the patch\n- Renamed request fields where structural rename was detected\n- Marked removed endpoint usages with `FIXME(mendpoint)` for human follow-up\n### E-graph migration exploration\n_Non-destructive equality saturation over localized API patterns (complements call-graph impact)._\n- `/v1/charges`: rules [rename_field_amount_cents_to_amount] · extracted `field(amount_cents)`\n- `/v1/charges`: rules [rename_field_amount_cents_to_amount] · extracted `field(amount_cents)`\n- `/v1/charges`: rules [rename_field_amount_cents_to_amount] · extracted `field(amount_cents)`\n### Review checklist\n- [ ] CI green\n- [ ] Business logic still correct after field renames\n- [ ] Removed endpoints replaced with supported alternatives\n---\n_Opened by **Fettler** (Mendpoint). Graph-leaned API impact · human review required · never auto-merged by default._\n_Change summary: [breaking] renamed request field amount_cents → amount on POST /v1/charg_\n### Plan: Warden spec plan: Acme Payments (1.0.0 → 2.0.0)\n\n- **Kind:** spec_diff\n- **Agent:** warden\n- **Goal:** Evolve Acme Payments API and migrate consumers safely for: [breaking] renamed request field amount_cents → amount on POST /v1/charges. removed /v1/charges/{id}/receipt. added /v1/balance. 4 other structural change(s).\n- **Progress:** 0/11 done\n\n| # | Step | Action | Status | Success criteria |\n|---|------|--------|--------|------------------|\n| 1 | Lock OpenAPI/Proto plan-of-record | `spec.lock_diff` | pending | Structural diff computed; Surfaces enumerated with severity; No code edits before plan steps complete or human approves |\n| 2 | Path /v1/charges/{id}/receipt removed | `spec.breaking_change` | pending | Consumer impact assessed via registry; Migration strategy: Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes.; Success criteria observable via contract tests or impact findings |\n| 3 | Path /v1/balance added | `spec.add_capability` | pending | Spec change documented; Migration strategy: Optional adoption: new capability at /v1/balance; Success criteria observable via contract tests or impact findings |\n| 4 | Request field amount_cents renamed to amount | `spec.evolve_field` | pending | Consumer impact assessed via registry; Migration strategy: Rename field usages amount_cents → amount on POST /v1/charges; Success criteria observable via contract tests or impact findings |\n| 5 | Response field amount_cents removed | `spec.breaking_change` | pending | Consumer impact assessed via registry; Migration strategy: Stop reading/writing field amount_cents; migrate consumers of that property.; Success criteria observable via contract tests or impact findings |\n| 6 | Response field amount added | `spec.add_capability` | pending | Spec change documented; Migration strategy: Optional adoption: new capability at POST /v1/charges; Success criteria observable via contract tests or impact findings |\n| 7 | Response field amount_cents removed | `spec.breaking_change` | pending | Consumer impact assessed via registry; Migration strategy: Stop reading/writing field amount_cents; migrate consumers of that property.; Success criteria observable via contract tests or impact findings |\n| 8 | Response field amount added | `spec.add_capability` | pending | Spec change documented; Migration strategy: Optional adoption: new capability at GET /v1/charges/{id}; Success criteria observable via contract tests or impact findings |\n| 9 | Run contract-conformance + breaking-change gates | `gate.contract_suite` | pending | oas-diff / surface checks pass or explicit break approved; Contract suite green or waivers recorded |\n| 10 | Fan-out consumer impact + generate reviewable PRs | `impact.fanout_prs` | pending | Consumers for affected surfaces listed; PRs opened with human review (never auto-merge by default) |\n| 11 | API design review (read-only critic) | `critic.api_reviewer` | pending | Idempotency, pagination, error taxonomy, versioning scored; Blocking issues attached to plan or waived by API owner |\n\n_Plan-of-record is editable state for the graph executor. Never auto-merges._\n### Consumer registry\n\nProvider **acme-payments** is monitored by **1** consumer(s):\n\n- **Shop** (`org/shop`) — `C:\\c\\tmp\\mp-slug\\fixtures\\consumers\\shop-app`\n\n_Query this registry before proposing breaking OpenAPI changes (Warden P0)._\n### Graph-RAG: blast_radius\nblast radius from change:87801000-ebd8-47a1-ad4d-8f40b570c0cb: 17 nodes, 32 edges\nCoverage: complete. This result enumerates everything within the query's scope.\n- (Change) [breaking] renamed request field amount_cents → amount on POST /v1/charges. removed /v1/charges/{id}/receipt. added /v1/ `change:87801000-ebd8-47a1-ad4d-8f40b570c0cb`\n- (Provider) tenant_default:acme-payments `provider:tenant_default:acme-payments`\n- (Surface) acme-payments./v1/charges/{id}/receipt.path_removed `surface:acme-payments./v1/charges/{id}/receipt.path_removed`\n- (Endpoint) ANY /v1/charges/{id}/receipt `endpoint:tenant_default:acme-payments:ANY:/v1/charges/{id}/receipt`\n- (Surface) acme-payments./v1/balance.path_added `surface:acme-payments./v1/balance.path_added`\n- (Surface) acme-payments.POST./v1/charges.request_field_renamed.amount_cents.amount `surface:acme-payments.POST./v1/charges.request_field_renamed.amount_cents.amount`\n- (Endpoint) POST /v1/charges `endpoint:tenant_default:acme-payments:POST:/v1/charges`\n- (Surface) acme-payments.POST./v1/charges.response_field_removed.amount_cents `surface:acme-payments.POST./v1/charges.response_field_removed.amount_cents`\n- (Surface) acme-payments.POST./v1/charges.response_field_added.amount `surface:acme-payments.POST./v1/charges.response_field_added.amount`\n- (Surface) acme-payments.GET./v1/charges/{id}.response_field_removed.amount_cents `surface:acme-payments.GET./v1/charges/{id}.response_field_removed.amount_cents`\n- (Endpoint) GET /v1/charges/{id} `endpoint:tenant_default:acme-payments:GET:/v1/charges/{id}`\n- (Surface) acme-payments.GET./v1/charges/{id}.response_field_added.amount `surface:acme-payments.GET./v1/charges/{id}.response_field_added.amount`\n- (5 more node(s) not shown here)\n### Warden PR gates\n\n- ❌ **oas-breaking-change**: 7 breaking surface(s): [breaking] renamed request field amount_cents → amount on POST /v1/charges. removed /v1/charges/{id}/receipt. added /v1/balance. 4 other structural change(s).\n- ✅ **contract-suite**: contract suite passed\n- ✅ **security-scan** _(attested, not verified)_: attested by caller — NOT independently verified (no security scanner runs in this pipeline)\n\n> ⚠️ Gates marked _(attested, not verified)_ reflect a caller-supplied assertion, not a check this pipeline ran. Treat them as unverified.\n\n_Gates failed — do not merge until fixed or waived by API owner._\n### Warden API reviewer (read-only critic)\n\n- **Score:** 77/100\n- **Summary:** API design review: 3 finding(s), score 77/100\n\n- **[warn]** `idempotency`: 1 POST operation(s) lack Idempotency-Key (or equivalent) documentation\n- **[warn]** `pagination`: 1 list-like GET path(s) lack pagination parameters\n- **[info]** `errors`: 4 operation(s) document no 4xx responses\n\n_Critic does not auto-merge. Style issues tests often miss (idempotency, pagination, errors, versioning)._\n### Policy\n- **Severity:** required\n- **Auto-merge:** disabled (draft package requires human merge)\n- auto-merge disabled (human review required)\n- **Labels:** mendpoint, needs-human-review, breaking-change\n### Immutable evidence\n- Candidate artifact: `artifact_6161f2b30deff3af465475a30f08ba8d239e0b8c8b58714fd25083e1b9a6f848`\n- Verification artifact: `artifact_f6612bfd5da7dbf33a1ade6ae12ad1a6a8672eff9171f4ab91272e5bf7ccb282`\n- Evidence record: `evidence_58383fb2b6dc2d00f8e38d351ff8255c756138043eeb0df72873952ee7e3d319`\n- Structured package artifact: `artifact_7b195f49442a6b5a0ea3fb09d8b03cbb2ed8bb14d3da83ae11c64952ae90a90b`\n\n### Structured Fettler draft package\n\n#### Summary\n[breaking] renamed request field amount_cents → amount on POST /v1/charges. removed /v1/charges/{id}/receipt. added /v1/balance. 4 other structural change(s).\n\n#### Why\nConfirmed 20 site(s); types: direct_call, http_path, field_access. Remove or replace calls to /v1/charges/{id}/receipt; follow provider migration notes. Optional adoption: new capability at /v1/balance Rename field usages amount_cents → amount on POST /v1/charges\n\n#### Exact files\n- `src/payments.ts`\n- `src/webhooks.py`\n\n#### Verification results\n- **contract-suite:** passed\n  - Evidence: `evidence_58383fb2b6dc2d00f8e38d351ff8255c756138043eeb0df72873952ee7e3d319`\n- **security-scan:** passed\n  - Evidence: `evidence_58383fb2b6dc2d00f8e38d351ff8255c756138043eeb0df72873952ee7e3d319`\n\n#### Risks\n- Change risk: breaking\n- Impact confidence: high\n- auto-merge disabled (human review required)\n\n#### Rollback\n- Strategy: `close_draft`\n- Instructions: Close the draft pull request and restore the exact files from the recorded repository snapshot before retrying.\n\n#### Evidence\n- Snapshot revision kind: `git_commit`\n- Snapshot revision: `7432e4a721d1d8e7f7c6e791ad15fb948f6ea18a`\n- `artifact_1f0647321cf1c2f207a2313a168fff3ff6c0193284826804d836cbc95a7b685b`\n- `artifact_775f1956bafc6921b41886e1703f1e3f87fac24f23d451db74ef7a2403c63a37`\n- `artifact_f6612bfd5da7dbf33a1ade6ae12ad1a6a8672eff9171f4ab91272e5bf7ccb282`\n- `evidence_58383fb2b6dc2d00f8e38d351ff8255c756138043eeb0df72873952ee7e3d319`\n\n#### Reviewer ownership\n- Owner: `github-owner:org`\n- Reviewer: `github-owner:org`\n- Required reviewer count: 1\n\n#### Delivery controls\n- Draft pull request only\n- Automatic merge: disabled\n- Automatic deployment: disabled\n- Package digest: `aa9348bcd652c74a86dbefcad82d096507be1241912756a261ad1f7e0f7875b1`",
    "branch": "mendpoint/acme-payments-190aa85fbe27c2c2",
    "deliveryKey": "87801000-ebd8-47a1-ad4d-8f40b570c0cb:ee4c7dc9-0c87-4fa6-ab09-793b02f3a209",
    "files": [
      {
        "path": "src/payments.ts",
        "content": "/** Shop checkout — uses Acme Payments (v1 patterns). */\n\nconst ACME_BASE = \"https://api.acme-payments.example\";\n\nexport async function chargeCustomer(cents: number, currency: string) {\n  const res = await fetch(`${ACME_BASE}/v1/charges`, {\n    method: \"POST\",\n    headers: { \"Content-Type\": \"application/json\" },\n    body: JSON.stringify({\n      amount: cents,\n      currency,\n      description: \"shop-app order\",\n    }),\n  });\n  if (!res.ok) throw new Error(`charge failed: ${res.status}`);\n  return res.json() as Promise<{ id: string; amount: number; status: string }>;\n}\n\nexport async function fetchReceipt(chargeId: string) {\n  const res = await fetch(`${ACME_BASE}/v1/charges/${chargeId}/receipt`);\n  if (!res.ok) throw new Error(`receipt failed: ${res.status}`);\n  return res.json() as Promise<{ url: string }>;\n}\n\nexport function acmeChargesCreate(client: {\n  charges: { create: (body: { amount: number; currency: string }) => Promise<unknown> };\n}, amount: number, currency: string) {\n  return client.charges.create({ amount, currency });\n}\n",
        "mode": "100644"
      },
      {
        "path": "src/webhooks.py",
        "content": "\"\"\"Webhook handlers that call Acme Payments.\"\"\"\n\nimport urllib.request\nimport json\n\nACME_BASE = \"https://api.acme-payments.example\"\n\n\ndef refund_hint(charge_id: str) -> dict:\n    # Looks up charge then historically pulled receipt\n    url = f\"{ACME_BASE}/v1/charges/{charge_id}/receipt\"\n    req = urllib.request.Request(url)\n    with urllib.request.urlopen(req) as resp:\n        return json.loads(resp.read().decode(\"utf-8\"))\n\n\ndef create_charge(amount: int, currency: str) -> dict:\n    payload = json.dumps({\"amount\": amount, \"currency\": currency}).encode()\n    req = urllib.request.Request(\n        f\"{ACME_BASE}/v1/charges\",\n        data=payload,\n        headers={\"Content-Type\": \"application/json\"},\n        method=\"POST\",\n    )\n    with urllib.request.urlopen(req) as resp:\n        return json.loads(resp.read().decode(\"utf-8\"))\n",
        "mode": "100644"
      }
    ]
  }
}